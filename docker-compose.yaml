version: '3.8'

services:
  azure-vm-manager:
    image: ghcr.io/crossgg/azure-vm-manager:latest
    container_name: azure-vm-manager
    ports:
      - "3000:3000"
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped