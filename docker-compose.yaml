version: '3.8'

services:
  cloud-vm-manager:
    image: ghcr.io/crossgg/cloud-vm-manager:latest
    container_name: cloud-vm-manager
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
    restart: unless-stopped
